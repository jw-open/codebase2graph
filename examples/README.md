# Examples

Generate a sample graph from the neighboring Claude Code source snapshot:

```bash
python -m code2graph ../claude-code-source-code --graph all --output examples/claude-code-source-code.graph.json --pretty
```

Large generated graph files are ignored by git by default.

